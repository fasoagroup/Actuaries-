<style>
.Box-row.Box-row--focus-gray.py-0.border-top { display: none !important; }
.pagehead { display: none !important; }
[data-testid="repository-header"] { display: none !important; }
</style>

# 🦅 Actuaries Repository

## Overview
This is the main wiki for the Actuaries project.

---

## JavaScript Example

```javascript
// Fetch repository data
async function getActuariesData() {
  try {
    const response = await fetch('https://api.github.com/repos/fasoagroup/Actuaries-');
    const data = await response.json();
    console.log('Repository:', data.name);
    console.log('Stars:', data.stargazers_count);
    console.log('Forks:', data.forks_count);
    return data;
  } catch (error) {
    console.error('Error fetching data:', error);
  }
}

getActuariesData();
```

## Python Example

```python
import requests
import json

def get_actuaries_repo_info():
    """Fetch Actuaries repository information from GitHub API"""
    url = "https://api.github.com/repos/fasoagroup/Actuaries-"
    
    try:
        response = requests.get(url)
        response.raise_for_status()
        data = response.json()
        
        print(f"Repository: {data['name']}")
        print(f"Description: {data['description']}")
        print(f"Stars: {data['stargazers_count']}")
        print(f"Forks: {data['forks_count']}")
        print(f"Language: {data['language']}")
        
        return data
    except requests.exceptions.RequestException as e:
        print(f"Error fetching repository: {e}")
        return None

if __name__ == "__main__":
    get_actuaries_repo_info()
```

## Quick Start

Run the Python script to get repository information:
```bash
python get_actuaries_info.py
```

Or use the JavaScript snippet in your browser console for quick stats.

---

**Last updated:** 2026-06-23
